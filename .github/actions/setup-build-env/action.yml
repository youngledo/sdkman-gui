name: 'Setup Build Environment'
description: 'Setup JDK 27 with JavaFX and Maven 4.0.0-rc-6'

runs:
  using: "composite"
  steps:
    - name: Set up JDK 27 with JavaFX
      uses: actions/setup-java@v4
      with:
        distribution: 'liberica'
        java-version: '27'
        java-package: 'jdk+fx'
        cache: 'maven'

    - name: Set up Maven 4.0.0-rc-6
      uses: stCarolas/setup-maven@v5
      with:
        maven-version: '4.0.0-rc-6'
